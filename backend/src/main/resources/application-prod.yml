spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/${DB_NAME:ecommerce}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

app:
  dataloader:
    enabled: true

logging:
  level:
    root: WARN
    com.datapulse: INFO
